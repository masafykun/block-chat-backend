# block-chat バックエンド設定のテンプレート。
# このファイルをコピーして .env を作り、実際のキーを入れる:
#     cp .env.example .env
# .env は .gitignore 済み。リポジトリにもチャットにも絶対に出さないこと。

OPENAI_API_KEY=sk-ここにあなたのOpenAI APIキー

# 任意: 使うモデル（未指定なら gpt-5.5）
# OPENAI_MODEL=gpt-5.4-mini
